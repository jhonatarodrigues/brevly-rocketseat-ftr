import { BrowserRouter } from "react-router-dom";
import RoutesSetup from "./routes";

function App() {
  return (
    <BrowserRouter>
      <RoutesSetup />
    </BrowserRouter>
  );
}

export default App;
